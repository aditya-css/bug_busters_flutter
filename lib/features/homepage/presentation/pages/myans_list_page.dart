import 'package:flutter/material.dart';

class MyAnsListPage extends StatelessWidget {
  const MyAnsListPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("My Answers List"),
    );
  }
}
